apiVersion: v1
kind: Service
metadata:
  name: {{ include "myfoo.fullname" . }}
  labels:
    {{- include "myfoo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myfoo.selectorLabels" . | nindent 4 }}

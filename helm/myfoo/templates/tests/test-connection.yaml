apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myfoo.fullname" . }}-test-connection"
  labels:
    {{- include "myfoo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myfoo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
